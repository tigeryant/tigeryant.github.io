{
  "name": "project-3-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "eslint-plugin-jsx-a11y": "^6.4.1",
    "eslint-plugin-react-hooks": "^4.2.0",
    "http-proxy-middleware": "^1.3.1",
    "react": "^17.0.2",
    "react-copy-to-clipboard": "^5.0.4",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "sass": "^1.38.0"
  },
    "scripts": {
      "start": "export SET NODE_OPTIONS=--openssl-legacy-provider && DISABLE_ESLINT_PLUGIN=true react-scripts start",
      "build": "export SET NODE_OPTIONS=--openssl-legacy-provider && DISABLE_ESLINT_PLUGIN=true react-scripts build",
      "test": "DISABLE_ESLINT_PLUGIN=true react-scripts test",
      "eject": "DISABLE_ESLINT_PLUGIN=true react-scripts eject",
      "dev": "DISABLE_ESLINT_PLUGIN=true react-scripts start"
    },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "eslint-config-prettier": "^8.5.0"
  }
}
